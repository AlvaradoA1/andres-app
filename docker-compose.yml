services:
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - ~/mongo/data:/data/db
  server:
    depends_on:
      - mongodb
    build: ./server
    command: npm start
    ports:
      - 5000:5000
    links:
      - mongodb